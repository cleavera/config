{
  "name": "@cleavera/config",
  "version": "1.2.0",
  "description": "Shared configuration for projects to keep consistent linting rules across projects",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Anthony Cleaver",
  "license": "ISC"
}
